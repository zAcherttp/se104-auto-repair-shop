# Integration Test Environment Variables Template

# Copy this file to .env.test.local and fill in your test database credentials
# DO NOT commit .env.test.local to version control

# Supabase Test Database Configuration
# Use a separate Supabase project for testing, or create a dedicated test schema
NEXT_PUBLIC_SUPABASE_URL=your_test_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_test_anon_key_here
NEXT_PUBLIC_SUPABASE_SERVICE_ROLE=your_test_service_role_key_here

# Notes:
# 1. Create a separate Supabase project for integration tests
# 2. Service role key is required for admin operations (user creation, RLS bypass)
# 3. Tests will automatically cleanup data after each run
# 4. Never use production database for testing
